{% extends 'base.html.twig' %}

{% block body %}
    <div class="p-5">
        <div class="text-2xl pb-10">
            <b>jameskitt616's Blog</b>
        </div>
        <div class="text-xl">
            {% for post in posts %}
                <div class="hover:underline cursor-pointer pb-2">
                    {{ post.title }}
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
